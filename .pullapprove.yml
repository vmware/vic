version: 2

group_defaults:
  approve_by_comment: true
  approve_regex: '^(Approved|:shipit:|:sheep::it:|:\+1:|LGTM)'
  reset_on_push:
    enabled: false

groups:
  build-ci-infra:
    conditions:
      files:
        - "tests/*"
      required: 1
      users:
        - gigawhitlocks
