# By default, packages in lib should depend only on packages under pkg or vendor
^pkg/*
^vendor/*

# lib/config subpackages can depend on each other
^lib/config/*

# BAD: lib/config depends on pkg/version, which depends on lib/migration/feature
^lib/migration/feature/*
