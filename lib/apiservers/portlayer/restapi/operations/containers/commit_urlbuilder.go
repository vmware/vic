package containers

// This file was generated by the swagger tool.
// Editing this file might prove futile when you re-run the generate command

import (
	"errors"
	"net/url"
	golangswaggerpaths "path"
	"strings"

	"github.com/go-openapi/swag"
)

// CommitURL generates an URL for the commit operation
type CommitURL struct {
	Handle string

	WaitTime *int32

	_basePath string
	// avoid unkeyed usage
	_ struct{}
}

// WithBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *CommitURL) WithBasePath(bp string) *CommitURL {
	o.SetBasePath(bp)
	return o
}

// SetBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *CommitURL) SetBasePath(bp string) {
	o._basePath = bp
}

// Build a url path and query string
func (o *CommitURL) Build() (*url.URL, error) {
	var result url.URL

	var _path = "/containers/{handle}"

	handle := o.Handle
	if handle != "" {
		_path = strings.Replace(_path, "{handle}", handle, -1)
	} else {
		return nil, errors.New("Handle is required on CommitURL")
	}
	_basePath := o._basePath
	if _basePath == "" {
		_basePath = "/"
	}
	result.Path = golangswaggerpaths.Join(_basePath, _path)

	qs := make(url.Values)

	var waitTime string
	if o.WaitTime != nil {
		waitTime = swag.FormatInt32(*o.WaitTime)
	}
	if waitTime != "" {
		qs.Set("wait_time", waitTime)
	}

	result.RawQuery = qs.Encode()

	return &result, nil
}

// Must is a helper function to panic when the url builder returns an error
func (o *CommitURL) Must(u *url.URL, err error) *url.URL {
	if err != nil {
		panic(err)
	}
	if u == nil {
		panic("url can't be nil")
	}
	return u
}

// String returns the string representation of the path with query string
func (o *CommitURL) String() string {
	return o.Must(o.Build()).String()
}

// BuildFull builds a full url with scheme, host, path and query string
func (o *CommitURL) BuildFull(scheme, host string) (*url.URL, error) {
	if scheme == "" {
		return nil, errors.New("scheme is required for a full url on CommitURL")
	}
	if host == "" {
		return nil, errors.New("host is required for a full url on CommitURL")
	}

	base, err := o.Build()
	if err != nil {
		return nil, err
	}

	base.Scheme = scheme
	base.Host = host
	return base, nil
}

// StringFull returns the string representation of a complete url
func (o *CommitURL) StringFull(scheme, host string) string {
	return o.Must(o.BuildFull(scheme, host)).String()
}
